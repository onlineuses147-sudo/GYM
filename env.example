import { Dumbbell, Instagram, MapPin } from 'lucide-react';

export default function Footer() {
  return (
    <footer className="bg-dark-900 pt-16 pb-8 border-t border-white/5 relative overflow-hidden">
      {/* Decorative */}
      <div className="absolute bottom-0 right-0 w-[500px] h-[500px] bg-brand-primary/5 rounded-full blur-[150px] pointer-events-none" />

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-12 mb-12 border-b border-white/10 pb-12">
          
          {/* Brand */}
          <div className="space-y-4">
            <a href="#home" className="flex items-center gap-2">
              <Dumbbell className="text-brand-primary w-8 h-8" />
              <span className="font-heading text-2xl font-bold tracking-wider text-white">
                MAHADEV <span className="text-brand-primary">FITNESS</span>
              </span>
            </a>
            <p className="text-gray-400 max-w-sm mt-4">
              Khammam's premier fitness destination offering world-class equipment, certified trainers, and a motivational environment.
            </p>
            <div className="flex gap-4 mt-6">
              <a href="#" className="w-10 h-10 bg-dark-800 rounded-full flex items-center justify-center text-gray-400 hover:text-brand-primary hover:bg-white/5 transition-colors border border-white/5">
                <Instagram size={20} />
              </a>
              <a href="https://maps.google.com/?q=Mahadev+Fitness+And+Gym,+Khammam" target="_blank" rel="noopener noreferrer" className="w-10 h-10 bg-dark-800 rounded-full flex items-center justify-center text-gray-400 hover:text-brand-primary hover:bg-white/5 transition-colors border border-white/5">
                <MapPin size={20} />
              </a>
            </div>
          </div>

          {/* Quick Links */}
          <div>
            <h4 className="text-white font-bold text-lg mb-6">Quick Links</h4>
            <ul className="space-y-3">
              {['Home', 'About', 'Services', 'Pricing', 'Gallery', 'Contact'].map((link) => (
                <li key={link}>
                  <a href={`#${link.toLowerCase()}`} className="text-gray-400 hover:text-brand-primary transition-colors">
                    {link}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          {/* CTA Banner */}
          <div>
             <div className="bg-gradient-to-br from-dark-800 to-dark-900 p-6 rounded-2xl border border-brand-primary/20 shadow-[0_0_20px_rgba(220,38,38,0.05)]">
                <h4 className="text-white font-bold text-lg mb-2">Transform Your Life</h4>
                <p className="text-gray-400 text-sm mb-4">Join today and start your journey towards a healthier you.</p>
                <a href="#pricing" className="inline-block w-full text-center py-3 bg-brand-primary text-white font-bold rounded-xl hover:bg-white transition-colors">
                  View Memberships
                </a>
             </div>
          </div>

        </div>

        <div className="flex flex-col md:flex-row justify-between items-center gap-4 text-gray-500 text-sm">
          <p>&copy; {new Date().getFullYear()} Mahadev Fitness & Gym. All rights reserved.</p>
          <p>Khammam, Telangana</p>
        </div>
      </div>
    </footer>
  );
}
